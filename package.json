{
  "name": "guidr",
  "private": true,
  "version": "0.0.1",
  "description": "Mobile first web app that creates self-guided audio tours around the users location",
  "author": "Team Balenciaga",
  "license": "BSD-2-Clause",
  "scripts": {
    "clean": "rimraf build/",
    "build": "npm run build:js && npm run build:css && npm run build:static",
    "build:css": "node-sass --source-map=true src/styles.scss build/bundle.css",
    "build:js": "webpack",
    "build:static": "cp -ru public/. build/",
    "watch": "parallelshell 'npm run watch:js -s' 'npm run watch:css -s' 'npm run watch:static -s'",
    "watch:js": "webpack --watch",
    "watch:css": "nodemon -q -w src/ --ext 'scss' --exec 'npm run build:css'",
    "watch:static": "nodemon -q -w public/ --exec 'npm run build:static'",
    "serve": "http-server -p 9090 -a 0.0.0.0 -c-1 build",
    "app-dev": "parallelshell 'npm run serve -s' 'npm run watch -s'"
  },
  "dependencies": {
    "classnames": "~2.1.3",
    "fetch-jsonp": "~0.9.2",
    "react": "~0.13.3",
    "react-gestures": "~0.1.8",
    "react-google-maps": "~2.1.0",
    "react-modal": "~0.3.0",
    "react-responsive-carousel": "^0.1.6",
    "uri-templates": "~0.1.7"
  },
  "devDependencies": {
    "babel-core": "~5.8.22",
    "babel-eslint": "~4.1.1",
    "babel-loader": "~5.3.2",
    "eslint": "~1.3.1",
    "file-loader": "~0.8.4",
    "github-webhook": "~1.1.2",
    "http-server": "~0.8.0",
    "live-reload": "~1.1.0",
    "node-sass": "~3.3.2",
    "nodemon": "~1.5.0",
    "parallelshell": "~2.0.0",
    "scss-lint": "0.0.0",
    "webpack": "~1.12.0"
  }
}
